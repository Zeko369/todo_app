# Migration `20200904203344-make-todo-required-on-task`

This migration has been generated by Fran Zekan at 9/4/2020, 10:33:44 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Task" ALTER COLUMN "todoId" SET NOT NULL
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200904133420-add-tasks..20200904203344-make-todo-required-on-task
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model Todo {
   id          Int       @default(autoincrement()) @id
@@ -24,10 +24,10 @@
 }
 model Task {
   id        Int       @default(autoincrement()) @id
-  todo      Todo?     @relation(fields: [todoId], references: [id])
-  todoId    Int?
+  todo      Todo      @relation(fields: [todoId], references: [id])
+  todoId    Int
   title     String
   checkedAt DateTime?
   createdAt DateTime  @default(now())
   updatedAt DateTime  @updatedAt
```


