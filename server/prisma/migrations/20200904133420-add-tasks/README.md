# Migration `20200904133420-add-tasks`

This migration has been generated by Fran Zekan at 9/4/2020, 3:34:20 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Task" (
"id" SERIAL,
"todoId" integer   ,
"title" text   NOT NULL ,
"checkedAt" timestamp(3)   ,
"createdAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"updatedAt" timestamp(3)   NOT NULL ,
PRIMARY KEY ("id")
)

ALTER TABLE "public"."Task" ADD FOREIGN KEY ("todoId")REFERENCES "public"."Todo"("id") ON DELETE SET NULL ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200904132730-add-archived-at-to-list..20200904133420-add-tasks
--- datamodel.dml
+++ datamodel.dml
@@ -3,26 +3,37 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model Todo {
   id          Int       @default(autoincrement()) @id
   title       String?
   description String?
   checked     Boolean   @default(false)
   checkedAt   DateTime?
-  createdAt   DateTime  @default(now())
+  tasks       Task[]
   list        List?     @relation(fields: [listId], references: [id])
   listId      Int?
   requires    Todo[]    @relation("Requirements", references: [id])
   requiredBy  Todo[]    @relation("Requirements", references: [id])
   tags        Tag[]     @relation("Tags", references: [id])
+  createdAt   DateTime  @default(now())
   updatedAt   DateTime  @updatedAt
 }
+model Task {
+  id        Int       @default(autoincrement()) @id
+  todo      Todo?     @relation(fields: [todoId], references: [id])
+  todoId    Int?
+  title     String
+  checkedAt DateTime?
+  createdAt DateTime  @default(now())
+  updatedAt DateTime  @updatedAt
+}
+
 model List {
   id         Int       @default(autoincrement()) @id
   title      String?
   todos      Todo[]
@@ -34,8 +45,8 @@
 model Tag {
   id        Int      @default(autoincrement()) @id
   text      String
   color     String?
+  todos     Todo[]   @relation("Tags", references: [id])
   createdAt DateTime @default(now())
   updatedAt DateTime @updatedAt
-  todos     Todo[]   @relation("Tags", references: [id])
 }
```


